# ============================================
# MongoDB 数据库配置
# ============================================

# 方式1: 使用完整的连接字符串（推荐）
# 格式: mongodb://[username:password@]host:port/database[?options]
# 
# 示例（无密码，本地开发）:
# MONGODB_URI=mongodb://localhost:27017/thinkspace
#
# 示例（有用户名和密码）:
# MONGODB_URI=mongodb://myuser:mypassword@localhost:27017/thinkspace?authSource=admin
#
# 示例（MongoDB Atlas 云数据库）:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/thinkspace?retryWrites=true&w=majority

MONGODB_URI=mongodb://localhost:27017/thinkspace

# ============================================
# 方式2: 使用单独的配置项（如果未设置 MONGODB_URI，将使用以下配置）
# ============================================

# MongoDB 主机地址
# MONGODB_HOST=localhost

# MongoDB 端口
# MONGODB_PORT=27017

# 数据库名称
# MONGODB_DATABASE=thinkspace

# MongoDB 用户名（如果数据库启用了认证，必须设置）
# MONGODB_USERNAME=your_username

# MongoDB 密码（如果数据库启用了认证，必须设置）
# MONGODB_PASSWORD=your_password

# 认证数据库（通常为 admin，如果使用默认值可省略）
# MONGODB_AUTH_SOURCE=admin
